# Admin password to access the upload panel
ADMIN_PASSWORD=your_secure_password_here

# Public base URL of your CDN (used to generate static links)
BASE_URL=https://upload.drilex.cz

# Port the backend listens on
PORT=3001

# Where uploaded files are stored (inside container, map a volume here)
UPLOADS_DIR=/app/uploads
